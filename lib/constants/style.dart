import 'package:flutter/material.dart';

const InputDecoration textField = InputDecoration(
                  border: OutlineInputBorder(),
                  hintText: 'Search Store',
                  prefixIcon: Icon(Icons.search),
                  fillColor: Color.fromRGBO(242, 243, 242, 100),
                  filled: true,
                );